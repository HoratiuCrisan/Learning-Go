Writing tests is just like writing functions, with a few rules:

the file need a name like: xxx_test.go
the test function starts with the word Test
the test function takes one argument only: t *testing.T
In order to use *testing.T you need to import "testing"
